import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import SalesPipelineTracker from './SalesPipelineTracker';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SalesPipelineTracker />
  </React.StrictMode>
);